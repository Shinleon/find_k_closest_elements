#include <unordered_map>

int main() {
  // create vector x (odd or even length, flip coin) and compute its median
  // split vector into vector 1, vector 2 (flip coin to find out if element will be in vector 1 or 2)
  // compute median

}

